# 模型服务接口对接文档

> 来源:Swagger UI(`/docs`)+ OpenAPI 定义(`/openapi.json`)+ **实测验证(2026-08-17)**。
> 本文档是 Node.js 后端 `asr.js` / `tts.js` 的编写依据。

---

## 1. ASR —— Paraformer Streaming ASR

- 基础地址:`http://192.168.110.247:50001`
- 健康检查:`GET /healthz`

### 接口:`POST /api/v1/asr_stream_file`

**用途**:上传一段音频,服务端按流式分块识别,返回中间结果 + 最终文本。

**请求**:
- Content-Type:`multipart/form-data`
- 字段:`file`(音频文件二进制,**必填**)

**返回**(实测):`application/json`

```json
{
  "result": [
    { "type": "partial", "text": "你", "accumulated": "你", "chunk_index": 0, "is_final": false },
    { "type": "partial", "text": "好", "accumulated": "你好", "chunk_index": 1, "is_final": false },
    { "type": "final",   "text": "几点了", "accumulated": "你好我是测试语音现在几点了", "chunk_index": 5, "is_final": true }
  ],
  "text": "你好我是测试语音现在几点了"
}
```

- **直接用 `text` 字段拿最终完整识别结果**,`result` 数组是流式过程,可忽略。

### 支持的音频格式(实测)

| 格式 | 结果 | 说明 |
|------|------|------|
| wav (16k / 8k, PCM s16le) | ✅ 200 | 最稳 |
| mp3 | ✅ 200 | 可用 |
| flac | ✅ 200 | 可用 |
| **webm** | ❌ 500 | **不支持** |
| 裸 PCM(无文件头) | ❌ 500 | **不支持** |

> ⚠️ **对设计的影响(关键)**:
> 浏览器 `getUserMedia` + `MediaRecorder` 默认产出 **webm**(Chrome)或 **mp4/aac**(Safari),
> 而 ASR **不吃 webm**。所以录音后必须**转码成 wav/mp3** 再上传——转码放在后端(Node 调 ffmpeg),
> 或前端用 Web Audio API 直接录 wav。**这是必须解决的一环,不是可选优化。**

---

## 2. TTS —— CosyVoice3 TTS API (DGX Spark)

- 基础地址:`http://192.168.110.247:50002`
- 健康检查:`GET /healthz`
- 所有接口均为 `multipart/form-data`。

### 2.1 音色管理

#### `GET /v1/voices` —— 列出已注册音色

实测返回:

```json
{ "voices": ["demo_voice", "assistant_voice", "string", "voice_test_01", "test_voice_02"] }
```

已有 5 个可用音色,`assistant_voice` 可直接用于合成。

#### `POST /v1/voices` —— 注册音色(一次性)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `spk_id` | string | ✅ | 音色 ID |
| `prompt_text` | string | ✅ | 参考音频对应文本 |
| `prompt_wav` | binary | ✅ | 参考音频 |
| `persist` | string | 否(默认 `"true"`) | 是否落盘 |

#### `DELETE /v1/voices/{spk_id}` —— 删除音色

| 参数 | 位置 | 类型 | 说明 |
|------|------|------|------|
| `spk_id` | path | string | 音色 ID |
| `persist` | query | boolean(默认 true) | 是否同时删磁盘 |

### 2.2 语音合成

#### `POST /inference_zero_shot` —— 零样本合成(推荐)

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tts_text` | string | ✅ | 合成文本 |
| `spk_id` | string | 否 | 已注册音色 ID(实测 `assistant_voice` 可用) |
| `prompt_text` / `prompt_wav` | — | 否 | 未注册音色时才需要 |
| `stream` | string | 否(默认 `"false"`) | 流式输出 |
| `speed` | number | 否(默认 `1.0`) | 语速 |

#### `POST /inference_cross_lingual` —— 跨语种合成

字段同 zero_shot(必填 `tts_text`)。

#### `POST /inference_instruct2` —— 指令控制合成

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tts_text` | string | ✅ | 合成文本 |
| `instruct_text` | string | ✅ | 指令文本(语气/情绪) |
| 其余 | — | 否 | 同前 |

### 返回格式(实测)——重要!

**合成接口不返回音频文件、也不返回 JSON,返回的是裸 PCM 流**:

- `Content-Type: application/octet-stream`(chunked)
- 响应头携带音频参数:
  - `x-audio-format: pcm_s16le`
  - `x-sample-rate: 24000`
  - `x-channels: 1`
- 实测 1.8 秒语音 = 86400 字节(24000 × 1.8 × 2 字节/采样)
- `stream=true` 时仍是裸 PCM,只是边生成边 chunked 发送,**无 SSE/JSON 封装**

> ⚠️ **对设计的影响(关键)**:
> 浏览器 `<audio>` 无法直接播放裸 PCM。后端收到 PCM 后必须**包成 wav(加 RIFF 头)**再返回给
> 浏览器播放。包 wav 头是纯 Node 十几行代码,不需要 ffmpeg。播放参数就是响应头那三个(24000/s16le/mono)。

---

## 3. LLM —— 语析服务(Yuxi)

- 基础地址:`http://192.168.110.139:5050`
- 健康检查:`GET /api/system/health` → `{"status":"ok","version":"0.7.1b2"}`
- **鉴权**:HTTP Header `Authorization: Bearer <apikey>`,不鉴权返回 401
  - API key:敏感信息,见本地 `server/config/local.json`(已 gitignore,不入库)
- **已配置 agent**:`default-chatbot`(智能助手,人设"聚能医工企业智能助手仕行智库",模型 `deepseek:deepseek-v4-flash`)——人设已由语析设好,我们只调接口

### 3.1 两个可用的对话接口(实测对比)

#### `POST /api/chat/call` —— 简单问答(同步)

```json
// 请求
{ "query": "你好，请用一句话介绍你自己" }
// 响应
{ "response": "你好！我是一个AI助手，可以帮助你...", "request_id": "..." }
```

- **同步**,一次调用直接拿结果
- ⚠️ **不带 agent 人设**(实测回复是通用 AI 助手,不是仕行智库人设)

#### `POST /api/agent/runs` —— 智能体调用(异步,推荐)

走完整 agent 流程,**带人设**,但需要三步:

```json
// ① 创建线程 POST /api/chat/thread
{ "agent_id": "default-chatbot" }
// → { "id": "0db5a6c4-...", ... }  拿到 thread_id

// ② 发起 run POST /api/agent/runs
{ "query": "你好，请用一句话介绍你自己",
  "agent_slug": "default-chatbot",
  "thread_id": "0db5a6c4-..." }
// → { "run_id": "35cfeb0f-...", "status": "pending", "stream_url": "/api/agent/runs/{run_id}/events" }

// ③ 轮询结果 GET /api/agent/runs/{run_id}
// → run.status: pending → completed
// ④ 取结果 GET /api/agent/runs/{run_id}/result
// → { "status": "completed", "output": "你好！我是聚能医工企业智能助手'仕行智库'，..." }
```

- 同步的 `chat/call` 不回人设;**要做带人设的语音助手,应走 `agent/runs` 异步链路**
- 多轮对话:复用同一 `thread_id` 即可保持上下文
- 实测完整链路 3 步全通,输出人设正确

### 3.2 LLM 接口选择建议

| 接口 | 同步/异步 | 带人设 | 多轮 | 结论 |
|------|----------|--------|------|------|
| `/api/chat/call` | 同步 | ❌ | ❓ | 简单调试用 |
| `/api/agent/runs` | 异步 | ✅ | ✅(复用 thread) | **正式链路用这个** |

---

## 4. 实测结论总览

| 项 | 结论 | 影响 |
|----|------|------|
| ASR 接口 | `POST /api/v1/asr_stream_file`,字段 `file` | 已确认 |
| ASR 返回 | JSON,`text` 字段为最终识别 | 直接用 `text` |
| ASR 格式 | **支持 wav/mp3/flac,不支持 webm/裸pcm** | **录音后必须转码** |
| TTS 接口 | 全部 multipart;已注册音色 `assistant_voice` 可用 | 已确认 |
| TTS 返回 | **裸 PCM(s16le/24k/mono)**,头带参数 | **后端需包 wav 头** |
| TTS stream | 仍裸 PCM,无封装 | 第一步不用 stream |
| LLM | 不可用,待排查 | 阻塞主链路 |

---

## 5. 下一步

1. **LLM**:拿到地址后先 `curl /healthz` + 一次文本请求排查。
2. 骨架代码按实测结果设计:
   - 录音转码:webm/mp4 → wav(后端 ffmpeg 或前端 Web Audio)
   - TTS 返回:裸 PCM → 后端包 wav 头 → 浏览器播放
3. 跑通"问一句答一句"闭环。
