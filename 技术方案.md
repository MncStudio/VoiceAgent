# VoiceAgent 技术方案

> 语音代理:浏览器录音 → ASR 识别 → LLM 生成回复 → TTS 合成 → 浏览器播放。
> 只写"调接口"的胶水代码,不碰任何模型底层。

---

## 1. 项目概述

| 项 | 值 |
|----|----|
| 项目名 | VoiceAgent(语音代理) |
| 语言 | 全 Node.js(后端)/ 原生 HTML+JS(前端) |
| 场景 | 浏览器录音提问,后端串行调用三个模型服务,返回语音回复 |
| 运行形态 | Node 服务 + Nginx HTTPS 反代 |

## 2. 技术栈

| 层 | 选型 |
|----|------|
| 后端 | Node.js 18+ / Express |
| 文件上传 | multer(收浏览器录音) |
| 模型调用 | 原生 `fetch`(三个服务都是 HTTP 接口) |
| 前端 | 单页 HTML:`getUserMedia` 录音 + `<audio>` 播放,不引框架 |
| 部署 | Nginx 反代 + SSL(麦克风要求 HTTPS) |

## 3. 架构

```
浏览器(录音 → 播放)
   │ HTTPS
Nginx(反向代理 + SSL)
   │
Node.js 后端(Express)
   ├── POST /api/chat   收音频(multer)
   ├── asr.js           → ASR 服务(音频 → 文字)
   ├── llm.js           → LLM 服务(文字 → 回复)
   ├── tts.js           → TTS 服务(回复 → 音频)
   └── 返回音频给浏览器播放
   │
┌──────────────┬──────────────┬──────────────┐
│ Paraformer ASR│   LLM 服务    │ CosyVoice3 TTS│
│ :50001       │  (地址待定)   │ :50002        │
└──────────────┴──────────────┴──────────────┘
```

## 4. 目录结构

```
voice-agent/
├── server/
│   ├── index.js     # Express 入口:路由 + 中间件 + 静态托管
│   ├── config.js    # 三个模型服务地址 / 超时 / 参数,集中配置
│   ├── asr.js       # 调 Paraformer ASR(音频 → 文字)
│   ├── llm.js       # 调 LLM(文字 → 回复)
│   ├── tts.js       # 调 CosyVoice3 TTS(回复 → 音频)
│   └── audio.js     # 临时音频文件处理:写、校验、清理
├── public/
│   └── index.html   # 录音 + 播放单页
├── package.json
├── .gitignore       # node_modules / tmp
└── 技术方案.md      # 本文档
```

## 5. 数据流(主链路)

```
1. 浏览器 getUserMedia 录音,得 webm/opus 音频
2. POST /api/chat  (multipart, 字段 audio)
3. asr.js  → POST :50001/api/v1/asr_stream_file  → 返回识别文字
4. llm.js  → POST <LLM 地址>                     → 返回回复文本
5. tts.js  → POST :50002/inference_zero_shot     → 返回回复音频
6. 后端返回音频 → 浏览器 <audio> 播放
```

## 6. 模型服务对接(已确认)

### 6.1 ASR —— Paraformer Streaming ASR (`:50001`)

- 接口:`POST /api/v1/asr_stream_file`
- 请求:`multipart/form-data`,字段 `file`(音频文件)
- 说明:服务端按流式分块喂音频,**返回识别中间结果(partials)**
- 接口地址:`http://192.168.110.247:50001`

> ⚠️ 待确认:返回的 JSON 结构(partial 字段名、final 判定)、支持的音频格式(webm? wav?)。

### 6.2 TTS —— CosyVoice3 TTS API (`:50002`)

接口地址:`http://192.168.110.247:50002`,全部 `multipart/form-data`:

| 接口 | 用途 | 必填字段 | 可选字段 |
|------|------|---------|---------|
| `POST /v1/voices` | 注册音色(一次性) | `spk_id`、`prompt_text`、`prompt_wav` | `persist`(默认 true) |
| `GET /v1/voices` | 列出已注册音色 | — | — |
| `DELETE /v1/voices/{spk_id}` | 删除音色 | path `spk_id` | query `persist` |
| `POST /inference_zero_shot` | 合成(推荐,已注册音色) | `tts_text` | `spk_id` / `prompt_text` / `prompt_wav` / `stream` / `speed` |
| `POST /inference_cross_lingual` | 跨语种合成 | `tts_text` | `prompt_wav` / `spk_id` / `stream` / `speed` |
| `POST /inference_instruct2` | 指令控制合成 | `tts_text`、`instruct_text` | `prompt_wav` / `spk_id` / `stream` / `speed` |

**推荐路线**:先用 `/v1/voices` 注册一次音色得到 `spk_id`,之后合成只传 `tts_text` + `spk_id`,省去每次传参考音频。

> ⚠️ 待确认:合成接口的**返回格式**(直接音频二进制? JSON base64? 文件名?)——决定 tts.js 怎么写。

### 6.3 LLM —— 待定

- 地址和接口格式**尚未提供**,`llm.js` 先用占位符,拿到接口后只改 `config.js` + `llm.js`。

## 7. 关键设计决定

| 决定 | 说明 |
|------|------|
| 单接口串行 | `POST /api/chat` 一次收音频,串行 ASR→LLM→TTS,先跑通闭环 |
| 音频格式 | 浏览器录音默认 **webm/opus**;若 ASR 要 wav,后端加转码(ffmpeg 或纯 JS) |
| 音色策略 | 走已注册 `spk_id`,不每次传参考音频 |
| 前端纯原生 | 单 HTML,不引 CDN、不引框架 |
| 临时文件 | 上传音频写 `/tmp` 一次性使用,用完删除,不落地 |

## 8. 待确认清单

- [ ] **LLM 服务地址和接口格式**(最重要,缺了跑不通)
- [ ] ASR 返回 JSON 结构 + 支持的音频格式(webm 直传 or 转 wav)
- [ ] TTS 合成返回格式(二进制 / base64 / 文件)
- [ ] 系统提示词 / LLM 人设
- [ ] 后端监听端口、是否要鉴权

## 9. 里程碑

- **M1 骨架**:Express + 三个 provider 占位 + 前端录音播放,串得通
- **M2 真实对接**:三个服务真实调用,跑通"问一句答一句"
- **M3 体验增强**(可选):SSE 流式、多轮对话记忆、音色管理页
